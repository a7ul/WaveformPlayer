import React from 'react';
import PropTypes from 'prop-types';
import * as styles from './style';

const CurrentPlaying = (props) => {
  const { song } = props;
  return (
    <styles.Container>
      <div />
    </styles.Container>
  );
};

CurrentPlaying.defaultProps = {

};

CurrentPlaying.propTypes = {

};

export default CurrentPlaying;
